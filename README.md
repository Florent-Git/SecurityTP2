Simplistic implementation of 3D Secure for a lesson.

## Running

The project can be run with following commands:

    $ ./gradlew acq:run
    $ ./gradlew acs:run
    $ ./gradlew acs-client:run
    $ ./gradlew sp:run

